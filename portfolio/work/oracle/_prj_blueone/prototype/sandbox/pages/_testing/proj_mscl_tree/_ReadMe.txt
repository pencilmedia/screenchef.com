------------------------------------------------------------	
FINITO
------------------------------------------------------------
PALETTE NAVIGATION BEHAVIORS
1) click on anything highlights
2) shift click is range select
3) alt click is multiple select
4) up/down is move
5) left/right is open/close
6) shift up/down is range sel



PALETTE ADD PILL BEHAVIORS
1) double click adds leaf (removed)
2) double click toggles folders
3) enter adds all selected



------------------------------------------------------------
FOR DISCUSSION - BEHAVIOR IS WEIRD
------------------------------------------------------------
1) if range select involves a parent AND one or more of its children, deselect the parent
2) select folder does not highlight children, but will add on enter
3) Pill range select and select all handled by engineering.


